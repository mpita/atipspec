---
name: atipspec-curate
description: AtipSpec phase curate. Use after a delivery, when audit or context report a document over its cap, or when the user asks to keep the project's context small.
---

# Phase curate

1. Run `atipspec curate` and do exactly what it prints: it regenerates the
   index, reports sizes and gives you the workflow to rewrite the prose.
2. End with `atipspec audit` clean.
